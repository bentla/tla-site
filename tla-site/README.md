# Méthode TLA — Site

Landing page one-page de la Méthode TLA (coaching boxe 60 jours).
Site statique : `index.html` + images. Aucune dépendance, aucun build.

## Hébergement
Site statique → déployable tel quel (Netlify, GitHub Pages, Vercel…).
Le dossier publié = ce repo (le dossier `_unused/` n'est pas suivi).

## Contact
contact@trainlikeathlete.fr
